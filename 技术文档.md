# Voice Sync - 手机语音输入直达电脑光标

## 📋 项目概述

这是一个局域网内的双向内容同步工具。手机可以把文字、图片和视频发送到电脑光标位置；电脑也可以把文字、图片、视频和普通文件发送到手机记录中查看、下载或转发。

### 核心功能
- 手机端网页输入文字 → 自动出现在电脑端光标位置
- 电脑端文字与附件 → 手机最近记录
- 手机端 AI 快捷按钮（API 密钥仅保存在电脑）
- 30 天持久记录、安全配对地址和附件下载
- 支持任何能输入文字的应用（记事本、Word、微信、浏览器、IDE等）
- 电脑端窗口置顶，可开关自动输入模式，重启需要双击确认

---

## 🏗️ 技术架构

```
┌─────────────┐     WebSocket      ┌──────────────┐     模拟粘贴      ┌──────────────┐
│  手机浏览器  │ ────────────────→ │  Flask 服务器 │ ────────────────→ │  电脑客户端   │
│  (sender)   │                    │  (server.py) │                    │  (client.py) │
└─────────────┘                    └──────────────┘                    └──────────────┘
      ↓                                   ↓                                   ↓
  输入文字                           广播消息                          复制到剪贴板
  点击发送                                                            模拟 Ctrl+V
                                                                      文字出现在光标处
```

---

## 🛠️ 技术栈

| 组件 | 技术 | 说明 |
|------|------|------|
| 服务器 | Flask + Flask-SocketIO | 处理 HTTP 和 WebSocket 通信 |
| 实时通信 | Flask-SocketIO + simple-websocket | threading 模式，无 eventlet 依赖 |
| 手机端 | HTML + Socket.IO Client | 纯网页，无需安装 App |
| 电脑客户端 | PyQt5 | 桌面 GUI 窗口 |
| 自动输入 | pyautogui + pyperclip | 模拟键盘输入 |

---

## 📁 项目文件结构

```
doubao-voice-assistant/
├── launcher.py         # 单实例启动器与服务进程管理
├── server.py           # Flask/Socket.IO 服务
├── client.py           # 电脑端 PyQt5 客户端
├── ai_assistant.py     # AI 设置、路由与接口调用
├── message_store.py    # 电脑到手机的记录与附件存储
├── transfer_config.py  # 共享限制、数据目录与安全配对凭证
├── requirements.txt    # 运行依赖
├── installer.iss       # Windows 安装包定义
├── templates/
│   └── sender.html     # 手机端网页
└── tests/              # 自动化回归测试
```

---

## 📦 依赖库 (requirements.txt)

```
flask==3.1.3
flask-socketio==5.6.1
simple-websocket==1.1.0
PyQt5==5.15.11
python-socketio[client]==5.16.3
pyautogui==0.9.54
pyperclip==1.11.0
```

---

## 🔧 关键技术实现

### 1. 服务器端 (server.py)

```python
from flask import Flask, render_template
from flask_socketio import SocketIO, emit

app = Flask(__name__)
socketio = SocketIO(app, async_mode='threading')

@app.route('/')
def sender():
    # 实际代码会校验电脑生成的安全配对令牌并写入 HttpOnly Cookie。
    return render_template('sender.html')

@socketio.on('send_text')
def handle_send_text(data):
    text = data.get('text', '')
    if text:
        emit('receive_text', {'text': text}, broadcast=True)

if __name__ == '__main__':
    socketio.run(app, host='0.0.0.0', port=56789)
```

### 2. 自动输入核心逻辑 (client.py)

```python
import pyperclip
import pyautogui

def on_text_received(self, text):
    if self.auto_input_mode:
        # 复制到剪贴板
        pyperclip.copy(text)
        # 模拟 Ctrl+V 粘贴
        pyautogui.hotkey('ctrl', 'v')
```

### 3. 手机端发送 (sender.html)

```javascript
const socket = io();

function sendText() {
    const text = textInput.value.trim();
    if (text && socket.connected) {
        socket.emit('send_text', { text: text });
        textInput.value = '';
    }
}
```

---

## 🚀 安装步骤

### 1. 环境准备
```bash
# 安装 Python 3.10+
# 下载地址: https://www.python.org/downloads/
# 安装时勾选 "Add Python to PATH"
```

### 2. 安装依赖
```bash
cd voice-sync
pip install -r requirements.txt
```

### 3. 启动应用
```bash
python launcher.py
```

正式使用建议运行安装包；启动器会负责单实例、后台服务和桌面窗口的生命周期。

### 4. 手机连接
- 确保手机和电脑在同一 WiFi
- 点击电脑端地址栏或复制按钮，复制包含安全配对信息的完整地址
- 把地址发送到手机并打开；首次验证后浏览器会保存配对 Cookie

---

## 💡 使用方法

1. 启动“语音输入助手”
2. 确保客户端窗口的 ⌨️ 按钮是**绿色**（自动输入开启）
3. 在电脑上点击你要输入文字的地方（光标就位）
4. 在手机上输入文字，点击发送
5. 文字自动出现在光标位置！

---

## ⚠️ 注意事项

1. **防火墙**: 首次使用可能需要允许语音输入助手通过防火墙
2. **同一网络**: 手机和电脑必须在同一局域网
3. **焦点位置**: 发送前确保电脑光标在目标输入框内
4. **剪贴板**: 自动输入会使用剪贴板，会覆盖原有内容
5. **VPN/代理**: 如果有 VPN 软件，可能需要关闭或配置排除局域网

---

## 🔍 常见问题

| 问题 | 解决方案 |
|------|---------|
| 手机访问不了 | 检查防火墙、确认同一WiFi、关闭VPN |
| PyQt6 报错 | 改用 PyQt5（已在代码中修改） |
| 自动输入无效 | 检查光标是否在输入框、⌨️ 按钮是否绿色 |
| 中文输入问题 | 使用剪贴板+粘贴方式，已解决 |

---

*文档版本: 2.0.1 | 更新日期: 2026-07*
